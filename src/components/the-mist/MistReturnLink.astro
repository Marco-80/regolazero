---
// Unico ponte visibile verso il sito madre dentro il microsito.
// Minimo e defilato (CLAUDE.md §7.2). Il sito madre è solo IT: si torna
// sempre alla root, ma l'etichetta segue la lingua del microsito.
import { t } from '../../i18n/the-mist';
import type { Lingua } from '../../i18n/the-mist';

interface Props {
  lang: Lingua;
}
const { lang } = Astro.props;
---

<a class="tm-return" href="/" data-pagefind-ignore>{t[lang].ritornoSitoMadre}</a>
