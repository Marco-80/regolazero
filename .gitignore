# asset sorgente grezzi (loghi ad alta risoluzione, ecc.) — solo le
# versioni ottimizzate per il web vanno versionate, sotto images/
/img/

# os / editor
.DS_Store
Thumbs.db
*.log

# stato locale di Claude Code, non va versionato
/.claude/
